1. Python files
    (1) mp4_2_wav : Convert .mp4 to .wav files.
    (2) vggish/extract_features : Extract mel features from wav by clips.
    (3) vggish/extract_embedding : Extract VGGish embedding from mel features and postprocess by clip.
    (4) Audio_transformer : The transformer model.

2. Data
    (1) Embeddings : The VGGish embeddings extracted by clip on XD-Violence.

# To save disk storage, I skipped the mp4, wav, and mel features files.